# ZjcGieDemo 代码问题清单（issues）

> 分析分支：`dev` · 分析时间：2026-09-16
> 范围：博客（`index.html` / `style.css` / `posts.js` / `main.js`）+ 排序算法（`sorting/*.js`）

## 概览

| 严重程度 | 数量 | 说明 |
|---------|------|------|
| 🔴 高 | 2 | 潜在安全/健壮性问题，建议优先修复 |
| 🟡 中 | 5 | 体验、可访问性、性能问题 |
| 🟢 低 | 6 | 整洁性、文档、测试覆盖 |

---

## 🔴 高优先级

### 1. 数据文本未转义直接拼入 innerHTML（潜在 XSS / 渲染错乱）

- **位置**：`main.js:78-90`、`main.js:103-104`
- **问题**：`renderPosts` 用模板字符串把 `p.title`、`p.category`、`p.summary`、`p.tags` 直接拼进 `innerHTML`；`openPost` 里 `detail-tags` 同样用 `innerHTML` 拼接标签。标题、摘要、标签本质是「内容」，一旦包含 `<`、`>`、`&` 等字符就会破坏 HTML 结构，甚至注入脚本。
- **当前影响**：`posts.js` 的数据是本地可信的，所以实际风险低，属于**潜伏问题**。但文章标题很容易写到带尖括号的字面量（例如「用 `<fetch>` 调接口」），会直接渲染错乱；一旦数据源变成接口/用户输入，就会升级为真实 XSS。
- **建议**：标题/摘要/标签改用 `textContent` 赋值，或统一走一个 `escapeHtml()` 转义函数；正文 `content` 作为受控 HTML 保留 `innerHTML`，但需加注释明确其信任边界。

### 2. localStorage 访问无异常保护，可能中断整个应用

- **位置**：`main.js:22`（读）、`main.js:33`（写）
- **问题**：`initTheme` 直接调用 `localStorage.getItem`。在部分环境（浏览器隐私模式、某些 `file://` 上下文、被禁用的存储）下会抛出 `SecurityError`。因为 `initTheme` 是 `init()` 的第一行（`main.js:119`），一旦抛错，后续 `renderTags` / `renderPosts` 都不会执行，**页面直接空白**。
- **建议**：把 localStorage 读写包一层 `try/catch`，或封装成安全的 `safeStorage` 工具，失败时回退到「跟随系统主题」。

---

## 🟡 中优先级

### 3. 深色模式首次加载闪烁（FOUC）

- **位置**：`main.js:21-27`、`main.js:130`、`index.html:43`
- **问题**：主题是通过 `DOMContentLoaded` 里的 `initTheme` 才设置的，而脚本又用了 `defer`。深色偏好的用户首次打开会先看到**白底一闪**再变黑。
- **建议**：在 `<head>` 里内联一段小脚本，在渲染前就设置 `data-theme`（同步读取 localStorage + 系统偏好），把 `main.js` 里的 `initTheme` 改为只负责更新按钮文案。

### 4. 文章卡片不可键盘操作（可访问性）

- **位置**：`main.js:80`（卡片结构）、`main.js:92-93`（绑定 click）
- **问题**：卡片是 `<article>`，只绑定了 `click`，没有 `tabindex`、`role` 或键盘事件。键盘用户无法聚焦卡片、也无法用回车打开文章。
- **建议**：卡片改用 `<button>` 语义，或加 `tabindex="0"` + `role="button"` + Enter/Space 键盘处理；配合问题 7 用事件委托会更简洁。

### 5. 深色模式下激活标签对比度不足

- **位置**：`style.css:97`
- **问题**：`.tag-btn.active` 固定用 `color: #fff`，但深色主题下 `--accent: #60a5fa`（`style.css:23`）是浅蓝，白字在浅蓝底上对比度约 2.3:1，不达 WCAG AA（需 ≥ 4.5:1）。
- **建议**：引入 `--accent-fg` 变量——浅色主题为白色、深色主题为深色文字，`.tag-btn.active` 用该变量。

### 6. 搜索无防抖，每次按键全量重渲染

- **位置**：`main.js:124-127`
- **问题**：`input` 事件每次触发都直接 `renderPosts`，无防抖。当前只有 6 篇数据无感，但数据量增长后连续输入会频繁重建 DOM。
- **建议**：加约 200ms 的 debounce。

### 7. 每次渲染重复绑定卡片事件，可用事件委托替代

- **位置**：`main.js:92-93`
- **问题**：`renderPosts` 每次重建 `#post-list` 的 innerHTML 后，都对所有 `.card` 逐个 `addEventListener`。功能正确但低效，且卡片越多绑定越多。
- **建议**：在 `#post-list` 上用一次事件委托（`closest('.card')`），避免每次重绑。

---

## 🟢 低优先级

### 8. 三个排序程序自测样板重复（DRY）

- **位置**：`sorting/bubble-sort.js:26-44`、`sorting/quick-sort.js:20-38`、`sorting/merge-sort.js:29-47`
- **问题**：三个文件的 `main()` 自测段几乎一模一样，复制了三份。
- **建议**：抽一个共享的 `runTests(sortFn)` 放到 `sorting/` 下公共模块，各程序只保留算法本体。

### 9. 快速排序选末尾为基准，已有序/倒序输入退化为 O(n²)

- **位置**：`sorting/quick-sort.js:10`
- **问题**：`const pivot = arr[arr.length - 1]`，倒序输入（自测样例 5）恰好就是最坏情况 O(n²)。
- **建议**：改用三数取中或随机基准；更彻底地可换成原地 partition 的实现（顺带解决下面的非原地问题）。

### 10. 排序自测覆盖不足

- **位置**：三个排序文件的 `samples`
- **问题**：样例只覆盖非负整数、空数组、单元素、倒序、含重复；缺少**负数**、**已有序**（冒泡最好情况未验证）、**大数据量随机数组**。
- **建议**：补负数与已有序样例，再加一组较大的随机数组校验长度与内容一致性。

### 11. 缺少 .gitignore

- **位置**：仓库根目录
- **问题**：macOS 下 `.DS_Store`、后续可能的 `node_modules/` 等容易被误提交。
- **建议**：新增 `.gitignore`，至少包含 `.DS_Store`、`node_modules/`、`*.log`。

### 12. README 未提及 sorting 目录

- **位置**：`README.md`
- **问题**：README 只描述了博客，`sorting/` 目录与运行方式完全没有体现，文档滞后。
- **建议**：补一节「排序算法」，列出三个文件与 `node sorting/xxx.js` 的运行方式。

### 13. 文章列表未按日期排序

- **位置**：`main.js:61-70`
- **问题**：`filterPosts` 保持 `POSTS` 的原始顺序，新增文章若不手动按日期倒序插入就会乱序。
- **建议**：在 `renderPosts` 前按 `date` 倒序排一次（或统一约定数据顺序并写进注释）。

---

## 总体评价

代码结构清晰、注释到位、纯原生无依赖、可读性好，作为学习/个人项目质量不错。核心逻辑（筛选、搜索、主题、三个排序算法）都能正确运行，排序程序自带自测且全部通过。

需要优先处理的是 **#1（转义）** 和 **#2（localStorage 容错）** 两个健壮性隐患；其次是 **#3（主题闪烁）** 和 **#4（键盘可访问性）**，这两点对用户体验和可访问性影响直观。其余多为整洁性与文档层面的改进。
